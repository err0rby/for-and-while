let arr = ["я", "не", "знаю", "что", "вам", "здесь", "такого", "нормального", "написать", "сегодня"];
for (let i = 0; i < arr.length; i++) {
    console.log (arr[i]);
}
let j = 0;
while (j < arr.length) {
    console.log (arr[j]);
    j++;
}