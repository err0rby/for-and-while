let arr = ["javascript", "css", "html", "что", "вам", "здесь", "такого", "нормального", "написать", "сегодня"];
let i = 0;
while (i < arr.length) {
    if (arr[i].length > 3) {
        console.log (arr[i]);
    }
    i++;
}